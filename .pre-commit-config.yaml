# Pre-commit hooks configuration
# See https://pre-commit.com for more information
#
# Install hooks: pre-commit install --hook-type pre-commit --hook-type commit-msg
# Run manually:  pre-commit run --all-files
# Skip hooks:    git commit --no-verify

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-added-large-files
      - id: check-case-conflict
      - id: check-executables-have-shebangs
      - id: check-merge-conflict
      - id: check-yaml
      - id: detect-private-key
      - id: end-of-file-fixer
      - id: mixed-line-ending
        args: [--fix=lf]
      - id: trailing-whitespace

  - repo: local
    hooks:
      - id: lint-skill-frontmatter
        name: Validate SKILL.md frontmatter
        entry: scripts/lint-skill-frontmatter.sh
        language: script
        files: SKILL\.md$

      - id: prettier
        name: Format markdown (prettier)
        entry: npx prettier --write --list-different
        language: system
        types: [markdown]
        exclude: CLAUDE\.md$

      - id: rust-format
        name: Rust format (cargo fmt --check)
        entry: scripts/check-rust-crates.sh fmt
        language: script
        files: ^.+/(Cargo\.toml|Cargo\.lock|src/.*\.rs|tests/.*\.rs)$

      - id: rust-clippy
        name: Rust lint (cargo clippy)
        entry: scripts/check-rust-crates.sh clippy
        language: script
        files: ^.+/(Cargo\.toml|Cargo\.lock|src/.*\.rs|tests/.*\.rs)$

      - id: rust-tests
        name: Rust tests (cargo test)
        entry: scripts/check-rust-crates.sh test
        language: script
        files: ^.+/(Cargo\.toml|Cargo\.lock|src/.*\.rs|tests/.*\.rs)$

      - id: mirror-invariants
        name: PR-side mirror invariants (byte-identity)
        entry: scripts/check-mirror-invariants.sh
        language: script
        pass_filenames: false
        files: ^ooda-(pr|prs|pr-codex-review)/src/.*\.rs$

      - id: validate-commit-message
        name: Validate commit subject (≤50 chars)
        entry: scripts/validate-commit-message.sh
        # No PR-suffix room needed: this repo doesn't squash-merge through
        # GitHub PRs. --max 50 disables the default 42/50 suffix-aware logic.
        args: ["--max", "50"]
        language: script
        stages: [commit-msg]
