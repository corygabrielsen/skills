[package]
name = "converge"
version = "0.1.0"
edition = "2024"
description = "Domain-agnostic observe→decide→act convergence loop"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ctrlc = "3"
